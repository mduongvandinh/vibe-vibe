---
title: "3.7.4 Luyện tập thực hành: Bài tập tổng hợp"
---

# 3.7.4 Luyện tập thực hành: Bài tập tổng hợp

Phần này cung cấp 4 bài tập tổng hợp giúp bạn nội hóa kỹ năng đã học trong chương. Đề nghị làm nghiêm túc từng bài tập, sau đó đối chiếu đáp án tham khảo để tự đánh giá.


## Bài tập 1: Viết lại Prompt

### Hướng dẫn

Dưới đây có 3 "Prompt kém", vui lòng dùng framework S.C.A.F.F. viết lại thành "Prompt tốt".

### Prompt kém 1

```
Giúp tôi làm một trang đăng nhập
```

**Bài viết lại của bạn**:

(Vui lòng tự thử viết lại trước, rồi mới xem đáp án tham khảo)

<details>
<summary>Click để xem đáp án tham khảo</summary>

```markdown
【Situation - Tình huống】
Tôi đang phát triển trang quản trị của blog cá nhân.
Tech stack: Next.js 16 + TypeScript + Tailwind CSS
Tiến độ hiện tại: Trang chủ và trang danh sách bài viết đã xong, bây giờ cần thêm chức năng đăng nhập
Phong cách thiết kế: Phong cách cực giản, tham khảo trang đăng nhập của Notion

【Challenge - Thử thách】
Implement trang đăng nhập quản trị viên:
- Form bao gồm: input box email, input box password, nút đăng nhập
- Click đăng nhập sau, validate format (format email, password không rỗng)
- Khi format sai, hiển thị thông báo màu đỏ dưới input box tương ứng
- Tạm không cần validate backend thật, làm giao diện frontend và tương tác trước

【Audience - Đối tượng】
- User: Chỉ có một người là tôi (quản trị viên blog)
- Người maintain code: Tôi, người mới học React
- Vui lòng thêm comment ở logic quan trọng

【Format - Định dạng】
Vui lòng cung cấp:
1. Code hoàn chỉnh component trang đăng nhập (file đơn)
2. Có type definition TypeScript
3. Style sử dụng Tailwind CSS
4. Tên file: LoginPage.tsx

【Foundations - Ràng buộc】
- Không dùng bất kỳ thư viện UI component nào
- Không dùng thư viện form bên thứ ba
- Quản lý state dùng useState gốc của React
- Thiết kế responsive: Cả mobile và desktop đều phải adapt
```

**Tiêu chuẩn chấm điểm**:
- ✅ Có background dự án và tech stack (2 điểm)
- ✅ Mô tả task cụ thể, có input output (2 điểm)
- ✅ Nói rõ đối tượng và trình độ kỹ thuật (1 điểm)
- ✅ Chỉ định format output (2 điểm)
- ✅ Có điều kiện ràng buộc rõ ràng (2 điểm)
- ✅ Sử dụng cấu trúc framework (1 điểm)

Tổng điểm 10, từ 8 điểm trở lên là xuất sắc.

</details>


### Prompt kém 2

```
Giúp tôi phân tích đoạn code này có vấn đề gì
```

**Bài viết lại của bạn**:

<details>
<summary>Click để xem đáp án tham khảo</summary>

```markdown
【Code hiện tại】
```typescript
const fetchUsers = async () => {
  const response = await fetch('/api/users');
  const data = response.json();
  setUsers(data);
}
```

【Hiện tượng vấn đề】
Gọi function này, state users là một Promise object thay vì array, dẫn đến render báo lỗi.

【Hành vi mong đợi】
users nên là mảng user lấy từ API

【Vui lòng giúp tôi】
1. Chỉ ra vấn đề trong code
2. Giải thích tại sao xuất hiện vấn đề này
3. Đưa ra code đã sửa
4. Nói cách tránh lỗi tương tự

【Về tôi】
Tôi là người mới học JavaScript, chưa rõ lắm về async/await
```

**Tiêu chuẩn chấm điểm**:
- ✅ Cung cấp code cụ thể (2 điểm)
- ✅ Mô tả hiện tượng vấn đề (2 điểm)
- ✅ Nói rõ hành vi mong đợi (2 điểm)
- ✅ Làm rõ cần giúp đỡ gì (2 điểm)
- ✅ Nói rõ trình độ kỹ thuật của mình (2 điểm)

Tổng điểm 10, từ 8 điểm trở lên là xuất sắc.

</details>


### Prompt kém 3

```
Tôi muốn làm phần mềm ghi chép chi tiêu
```

**Bài viết lại của bạn**:

<details>
<summary>Click để xem đáp án tham khảo</summary>

```markdown
【Situation - Tình huống】
Tôi muốn làm một ứng dụng web ghi chép chi tiêu cá nhân, giúp tôi theo dõi chi tiêu hàng ngày.
Tech stack: React + TypeScript + Tailwind CSS
Người dùng mục tiêu: Tôi (người chi tiêu hết lương, muốn biết tiền đi đâu)
Nguyên tắc thiết kế: Cực giản, ghi một khoản chi tiêu không quá 5 giây

【Challenge - Thử thách】
Version đầu (MVP) implement các chức năng sau:
1. Ghi nhanh chi tiêu: Chọn danh mục + Nhập số tiền + Ghi chú tùy chọn
2. Xem hóa đơn: Xem tất cả bản ghi trong ngày theo ngày
3. Tổng hợp tháng: Hiển thị tổng chi tiêu tháng này và tỷ lệ từng danh mục

【Audience - Đối tượng】
- User: Tôi, cần đơn giản nhanh chóng
- Người maintain code: Tôi, có một chút base React

【Format - Định dạng】
Đây là lần đối thoại đầu tiên, vui lòng giúp tôi trước:
1. Xác nhận phạm vi MVP này có hợp lý không
2. Gợi ý cấu trúc file của dự án
3. Liệt kê danh sách component cần implement

【Foundations - Ràng buộc】
- Data lưu trong localStorage trước, không cần backend
- Danh mục cố định: Ăn uống, Di chuyển, Mua sắm, Giải trí, Khác
- Chỉ ghi chi tiêu, không ghi thu nhập (đơn giản hóa)
- Không cần đăng nhập user

【Danh sách không làm】
- Không làm nhiều sổ chi tiêu
- Không làm chức năng ngân sách
- Không làm xuất dữ liệu
- Không làm biểu đồ visualization (version 2 cân nhắc)
```

**Tiêu chuẩn chấm điểm**:
- ✅ Có background dự án đầy đủ (2 điểm)
- ✅ Phạm vi chức năng MVP rõ ràng (2 điểm)
- ✅ Có "Danh sách không làm" (2 điểm)
- ✅ Yêu cầu lần đối thoại đầu hợp lý (quy hoạch chứ không phải viết code ngay) (2 điểm)
- ✅ Điều kiện ràng buộc cụ thể (2 điểm)

Tổng điểm 10, từ 8 điểm trở lên là xuất sắc.

</details>


## Bài tập 2: Viết PRD thực hành

### Hướng dẫn

Giả sử bạn muốn làm một công cụ "Ghi chú đọc sách", giúp bản thân ghi lại cảm nhận đọc sách.

Vui lòng tích hợp các công cụ tư duy Chương 2, hoàn thành các nội dung sau:

1. Dùng format JTBD mô tả user task
2. Dùng tư duy trừ xác định phạm vi MVP (P0/P1/Không làm)
3. Trả lời 3 câu hỏi linh hồn
4. Viết một bản PRD version rút gọn

### Câu trả lời của bạn

(Vui lòng tự hoàn thành trước, rồi mới xem đáp án tham khảo)

<details>
<summary>Click để xem đáp án tham khảo</summary>

### 1. Mô tả task JTBD

> Khi tôi đọc xong một chương nào đó của cuốn sách, tôi muốn nhanh chóng ghi lại quan điểm chính và suy nghĩ của mình, để khi cần ôn lại tôi có thể nhanh chóng tìm thấy những ghi chú này, thay vì phải lật lại sách.

### 2. Phạm vi MVP

| Ưu tiên | Chức năng | Lý do |
|-------|------|------|
| **P0** | Tạo ghi chú (Tên sách + Chương + Nội dung) | Chức năng cốt lõi |
| **P0** | Xem danh sách tất cả ghi chú | Chức năng cốt lõi |
| **P0** | Lọc ghi chú theo tên sách | Tìm ghi chú phải nhanh |
| **P1** | Sửa ghi chú có sẵn | Thường cần sửa bổ sung |
| **P1** | Xóa ghi chú | Dọn ghi chú vô dụng |
| **Không làm** | Tag/Phân loại ghi chú | Tăng độ phức tạp, MVP không cần |
| **Không làm** | Chia sẻ ghi chú | Dùng cá nhân, không cần |
| **Không làm** | Editor Markdown | Text đơn giản là đủ |
| **Không làm** | Tự động lấy thông tin sách | Nhập tay tên sách cũng được |

### 3. 3 câu hỏi linh hồn

| Câu hỏi | Trả lời |
|-----|------|
| **User là ai?** | Tôi - người thích đọc sách nhưng hay quên nội dung sách |
| **Pain point ở đâu?** | Sách đọc rồi nhớ không được, khi cần không tìm được suy nghĩ lúc đó |
| **Tại sao làm cái này?** | Công cụ hiện có (Notion, note) hoặc quá nặng, hoặc khó tìm theo sách |

### 4. PRD version rút gọn

```markdown
# PRD Công cụ ghi chú đọc sách

## Background dự án

### Vấn đề
Tôi thích đọc sách nhưng hay quên sau khi đọc. Khi cần ôn lại, hoặc phải lật lại sách, hoặc tìm ghi chú lúc đó trong các app khác nhau, rất tốn thời gian.

### JTBD
Khi tôi đọc xong một chương nào đó của cuốn sách, tôi muốn nhanh chóng ghi lại quan điểm chính và suy nghĩ của mình, để khi cần ôn lại tôi có thể nhanh chóng tìm thấy những ghi chú này.

## Người dùng mục tiêu

- Định danh: User cá nhân thích đọc sách
- Kịch bản: Ghi lại sau khi đọc xong một chương, tìm kiếm khi cần trích dẫn
- Nhu cầu cốt lõi: Ghi nhanh + Tìm nhanh

## Chức năng P0

### Chức năng 1: Tạo ghi chú
- Bắt buộc: Tên sách, Nội dung ghi chú
- Tùy chọn: Tên chương, Số trang
- Tương tác: Mở trang ra có thể bắt đầu viết ngay

### Chức năng 2: Xem danh sách ghi chú
- Hiển thị theo thứ tự thời gian tạo giảm dần
- Mỗi mục hiển thị: Tên sách, Chương, Tóm tắt nội dung, Thời gian
- Click có thể xem nội dung đầy đủ

### Chức năng 3: Lọc theo tên sách
- Đầu trang có bộ lọc tên sách
- Sau khi chọn chỉ hiển thị ghi chú của sách đó

## Phương án kỹ thuật

- Frontend: React + TypeScript + Tailwind CSS
- Data: localStorage (dùng cá nhân, không cần backend)
- Responsive: Hỗ trợ điện thoại và máy tính

## Danh sách không làm

- Không làm tag/phân loại ghi chú
- Không làm chức năng chia sẻ ghi chú
- Không làm editor Markdown
- Không làm tự động lấy bìa/thông tin sách
- Không làm đồng bộ đa thiết bị

## Tiêu chuẩn thành công

Tự mình dùng 2 tuần, ghi ít nhất 10 ghi chú, và thành công tìm kiếm được ghi chú trước đó.
```

**Tiêu chuẩn tự đánh giá**:
- ✅ Mô tả JTBD đầy đủ (Kịch bản + Task + Giá trị)
- ✅ Phạm vi MVP có ưu tiên, có "Danh sách không làm"
- ✅ 3 câu hỏi linh hồn đều trả lời được
- ✅ Cấu trúc PRD đầy đủ, AI có thể hiểu và thực thi

</details>


## Bài tập 3: Mô phỏng đối thoại lặp lại

### Hướng dẫn

Giả sử bạn nhờ AI giúp implement chức năng tạo "ghi chú đọc sách", AI cho bạn code sau, nhưng có vấn đề.

Vui lòng mô phỏng 3 vòng đối thoại lặp lại, từng bước sửa vấn đề.

### Output lần đầu của AI

```typescript
const CreateNote = () => {
  const [bookName, setBookName] = useState('');
  const [content, setContent] = useState('');

  const handleSubmit = () => {
    const note = { bookName, content };
    const notes = JSON.parse(localStorage.getItem('notes'));
    notes.push(note);
    localStorage.setItem('notes', JSON.stringify(notes));
  }

  return (
    <form onSubmit={handleSubmit}>
      <input value={bookName} onChange={e => setBookName(e.target.value)} />
      <textarea value={content} onChange={e => setContent(e.target.value)} />
      <button type="submit">Lưu</button>
    </form>
  );
}
```

### Nhận diện vấn đề

Vui lòng tìm ra vấn đề của đoạn code này, sau đó viết feedback của bạn.

<details>
<summary>Click để xem phân tích vấn đề và ví dụ lặp lại</summary>

### Danh sách vấn đề

1. **localStorage.getItem có thể trả về null**: Lần đầu sử dụng sẽ báo lỗi
2. **Không tạo note id**: Sửa/xóa sau này cần id
3. **Không ghi thời gian tạo**: Danh sách cần sắp xếp theo thời gian
4. **Form submit xong không xóa input**: Trải nghiệm user kém
5. **Không validate form**: Nội dung rỗng vẫn submit được
6. **handleSubmit không ngăn hành vi mặc định**: Form sẽ refresh trang

### Feedback vòng 1

```markdown
【Vấn đề】
1. localStorage.getItem('notes') lần đầu sẽ trả về null, dẫn đến notes.push báo lỗi
2. handleSubmit không có e.preventDefault(), form sẽ refresh trang

【Lý do】
localStorage lần đầu dùng không có data, JSON.parse(null) sẽ lỗi

【Kỳ vọng】
1. Xử lý trường hợp localStorage rỗng, khởi tạo thành mảng rỗng
2. Ngăn hành vi submit mặc định của form
```

### Output lần 2 của AI

```typescript
const handleSubmit = (e: React.FormEvent) => {
  e.preventDefault();
  const note = { bookName, content };
  const existingNotes = localStorage.getItem('notes');
  const notes = existingNotes ? JSON.parse(existingNotes) : [];
  notes.push(note);
  localStorage.setItem('notes', JSON.stringify(notes));
}
```

### Feedback vòng 2

```markdown
【Vấn đề】
1. Ghi chú không có id, sau này không thể sửa hoặc xóa ghi chú cụ thể
2. Không có thời gian tạo, danh sách không thể sắp xếp theo thời gian

【Kỳ vọng】
Mỗi ghi chú cần chứa:
- id: Dùng Date.now() hoặc uuid
- createdAt: Timestamp thời gian tạo
- bookName: Tên sách
- content: Nội dung
```

### Output lần 3 của AI

```typescript
const handleSubmit = (e: React.FormEvent) => {
  e.preventDefault();
  const note = {
    id: Date.now().toString(),
    bookName,
    content,
    createdAt: new Date().toISOString()
  };
  const existingNotes = localStorage.getItem('notes');
  const notes = existingNotes ? JSON.parse(existingNotes) : [];
  notes.push(note);
  localStorage.setItem('notes', JSON.stringify(notes));
}
```

### Feedback vòng 3

```markdown
【Vấn đề】
1. Lưu thành công không xóa input box
2. Không validate form, tên sách và nội dung rỗng vẫn submit được
3. Không có thông báo phản hồi lưu thành công

【Kỳ vọng】
1. Lưu thành công thì xóa bookName và content
2. Tên sách và nội dung không được rỗng (sau khi loại bỏ khoảng trắng mới kiểm tra)
3. Lưu thành công hiển thị thông báo ngắn (như toast)

【Ví dụ】
Sau khi lưu thành công:
- Input box xóa sạch
- Hiển thị thông báo "Ghi chú đã lưu" 2 giây sau biến mất
```

### Code cuối cùng

```typescript
const CreateNote = () => {
  const [bookName, setBookName] = useState('');
  const [content, setContent] = useState('');
  const [showToast, setShowToast] = useState(false);

  const handleSubmit = (e: React.FormEvent) => {
    e.preventDefault();

    // Validate form
    if (!bookName.trim() || !content.trim()) {
      return;
    }

    const note = {
      id: Date.now().toString(),
      bookName: bookName.trim(),
      content: content.trim(),
      createdAt: new Date().toISOString()
    };

    const existingNotes = localStorage.getItem('notes');
    const notes = existingNotes ? JSON.parse(existingNotes) : [];
    notes.push(note);
    localStorage.setItem('notes', JSON.stringify(notes));

    // Xóa input và hiển thị thông báo
    setBookName('');
    setContent('');
    setShowToast(true);
    setTimeout(() => setShowToast(false), 2000);
  }

  return (
    <>
      <form onSubmit={handleSubmit}>
        <input
          value={bookName}
          onChange={e => setBookName(e.target.value)}
          placeholder="Tên sách"
        />
        <textarea
          value={content}
          onChange={e => setContent(e.target.value)}
          placeholder="Nội dung ghi chú"
        />
        <button type="submit">Lưu</button>
      </form>
      {showToast && <div className="toast">Ghi chú đã lưu</div>}
    </>
  );
}
```

**Điểm cốt lõi lặp lại**:
- Mỗi vòng chỉ feedback 2-3 vấn đề, đừng liệt kê hết tất cả vấn đề một lần
- Format feedback: Vấn đề + Lý do + Kỳ vọng
- Vấn đề quan trọng (như báo lỗi) ưu tiên xử lý

</details>


## Bài tập 4: Nhận diện và sửa Hallucination

### Hướng dẫn

Giả sử AI cho bạn đoạn code snippet sau. Vui lòng nhận diện vấn đề hallucination trong đó, và nói cách xác minh cũng như sửa.

### Output của AI

```typescript
// AI gợi ý dùng thư viện super-easy-storage để đơn giản hóa thao tác localStorage
import { createStore, useStore } from 'super-easy-storage';

const notesStore = createStore('notes', []);

const CreateNote = () => {
  const [notes, setNotes] = useStore(notesStore);

  // Dùng array.findLast() lấy ghi chú cuối cùng
  const lastNote = notes.findLast(note => note.bookName === currentBook);

  // Dùng useTransition của React 18 xử lý danh sách lớn
  const [isPending, startTransition] = useTransition();

  const handleSubmit = () => {
    startTransition(() => {
      setNotes([...notes, newNote]);
    });
  }
}
```

### Câu trả lời của bạn

Vui lòng nhận diện vấn đề hallucination trong code trên, và nói phương pháp xác minh.

<details>
<summary>Click để xem đáp án tham khảo</summary>

### Nhận diện Hallucination

| Vị trí code | Loại vấn đề | Mô tả vấn đề | Phương pháp xác minh |
|---------|---------|---------|---------|
| `super-easy-storage` | 🔴 Package hallucination | Package này có thể không tồn tại | `npm search super-easy-storage` hoặc truy cập npmjs.com |
| `array.findLast()` | 🟡 API hallucination | API mới ES2023, môi trường cũ không hỗ trợ | Tra docs MDN xác nhận tính tương thích |
| `useTransition` | ✅ Đúng | React 18 thực sự có Hook này | Nhưng cần xác nhận dự án dùng React 18 |

### Các bước xác minh

**1. Xác minh package super-easy-storage**

```bash
npm search super-easy-storage
```

Kết quả: Không tìm thấy package này. Đây là **Package hallucination**.

**Phương án sửa**:
- Dùng thư viện tồn tại thực sự như `zustand` hoặc `jotai`
- Hoặc tự đóng gói một localStorage hook đơn giản

**2. Xác minh findLast() API**

Truy cập [MDN - Array.findLast()](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Array/findLast)

Kết quả:
- Được giới thiệu trong ES2023
- Chrome 97+, Firefox 104+, Safari 15.4+
- Node.js 18+

**Phương án sửa**:
```typescript
// Cách viết tương thích
const lastNote = [...notes].reverse().find(note => note.bookName === currentBook);

// Hoặc thêm polyfill
```

**3. Xác minh useTransition**

Truy cập [React docs - useTransition](https://react.dev/reference/react/useTransition)

Kết quả: React 18 thực sự có Hook này.

**Cần xác nhận**:
- Phiên bản React của dự án có >= 18 không
- Tình huống hiện tại có thực sự cần useTransition không (có thể là tối ưu quá mức)

### Code sau khi sửa

```typescript
// Dùng zustand thay thế super-easy-storage không tồn tại
import { create } from 'zustand';
import { persist } from 'zustand/middleware';

interface NotesState {
  notes: Note[];
  addNote: (note: Note) => void;
}

const useNotesStore = create<NotesState>()(
  persist(
    (set) => ({
      notes: [],
      addNote: (note) => set((state) => ({
        notes: [...state.notes, note]
      })),
    }),
    { name: 'notes-storage' }
  )
);

const CreateNote = () => {
  const { notes, addNote } = useNotesStore();

  // Dùng cách viết tương thích thay findLast
  const lastNote = [...notes]
    .reverse()
    .find(note => note.bookName === currentBook);

  const handleSubmit = () => {
    addNote(newNote);
  }
}
```

### Tổng kết thói quen xác minh

1. **Gặp package không quen**: npm search xác minh tồn tại trước
2. **Gặp API không quen**: Tra MDN hoặc tài liệu chính thức
3. **Gặp syntax mới**: Xác nhận môi trường dự án có hỗ trợ không
4. **Gặp "cách dùng cao cấp"**: Xác minh trước có thực sự cần không

</details>


## Bảng tự đánh giá bài tập

Sau khi hoàn thành 4 bài tập, dùng bảng này tự đánh giá:

| Bài tập | Tình hình hoàn thành | Thu hoạch chính | Chỗ cần tăng cường |
|-----|---------|---------|---------------|
| Bài tập 1: Viết lại Prompt | □ Hoàn thành □ Hoàn thành một phần □ Chưa hoàn thành | | |
| Bài tập 2: Viết PRD | □ Hoàn thành □ Hoàn thành một phần □ Chưa hoàn thành | | |
| Bài tập 3: Đối thoại lặp lại | □ Hoàn thành □ Hoàn thành một phần □ Chưa hoàn thành | | |
| Bài tập 4: Nhận diện Hallucination | □ Hoàn thành □ Hoàn thành một phần □ Chưa hoàn thành | | |


## Điểm cốt lõi phần này

✓ **Viết lại Prompt**: Dùng framework S.C.A.F.F. chuyển nhu cầu mơ hồ thành prompt có cấu trúc

✓ **Viết PRD**: Tích hợp thành quả tư duy Chương 2, tạo task brief AI hiểu được

✓ **Đối thoại lặp lại**: Mỗi vòng feedback 2-3 vấn đề, format là "Vấn đề + Lý do + Kỳ vọng"

✓ **Nhận diện Hallucination**: Với package và API không quen, xác minh trước khi dùng


Chúc mừng bạn đã hoàn thành toàn bộ nội dung học tập Chương 3! Phần tiếp theo chúng ta sẽ kiểm kê kỹ năng đã học, và chuẩn bị cho thực hành phát triển Chương 4.
