---
title: "3.8.4 Người nước ngoài có dùng được không——Internationalization: i18n framework và hỗ trợ đa ngôn ngữ"
---

# 3.8.4 Internationalization i18n

### Tóm tắt một câu

Internationalization là kiến trúc cho phép code hỗ trợ đa ngôn ngữ, không phải chỉ đơn giản là dịch tiếng Trung sang tiếng Anh.

### Giá trị cốt lõi

Internationalization (i18n = internationalization, 18 ký tự giữa chữ đầu và cuối) là tách văn bản ra khỏi code, cho phép sản phẩm hỗ trợ bất kỳ ngôn ngữ nào mà không cần sửa code.

### Next.js + next-intl approach

```bash
npm install next-intl
```

**Folder structure**:

```
src/
├── app/
│   └── [locale]/
│       ├── layout.tsx
│       └── page.tsx
├── messages/
│   ├── en.json
│   └── zh.json
└── i18n.ts
```

**Config files**:

```ts
// i18n.ts
import { getRequestConfig } from 'next-intl/server'

export default getRequestConfig(async ({ locale }) => ({
  messages: (await import(`./messages/${locale}.json`)).default
}))

// middleware.ts
import createMiddleware from 'next-intl/middleware'

export default createMiddleware({
  locales: ['en', 'zh'],
  defaultLocale: 'zh'
})

export const config = {
  matcher: ['/((?!api|_next|.*\\..*).*)']
}
```

### Translation files

```json
// messages/zh.json
{
  "common": {
    "submit": "Gửi",
    "cancel": "Hủy",
    "loading": "Đang tải...",
    "error": "Có lỗi xảy ra"
  },
  "home": {
    "title": "Chào mừng đến website của chúng tôi",
    "description": "Đây là mô tả mẫu"
  },
  "auth": {
    "login": "Đăng nhập",
    "logout": "Đăng xuất",
    "welcome": "Chào mừng trở lại, {name}!"
  }
}

// messages/en.json
{
  "common": {
    "submit": "Submit",
    "cancel": "Cancel",
    "loading": "Loading...",
    "error": "Something went wrong"
  },
  "home": {
    "title": "Welcome to our website",
    "description": "This is a sample description"
  },
  "auth": {
    "login": "Login",
    "logout": "Logout",
    "welcome": "Welcome back, {name}!"
  }
}
```

### Sử dụng trong component

```tsx
// app/[locale]/page.tsx
import { useTranslations } from 'next-intl'

export default function HomePage() {
  const t = useTranslations('home')
  const tCommon = useTranslations('common')

  return (
    <div>
      <h1>{t('title')}</h1>
      <p>{t('description')}</p>
      <button>{tCommon('submit')}</button>
    </div>
  )
}

// Có tham số
function WelcomeMessage({ name }: { name: string }) {
  const t = useTranslations('auth')

  return <p>{t('welcome', { name })}</p>
  // Output: "Chào mừng trở lại, John!" hoặc "Welcome back, John!"
}
```

### Plural và gender

```json
// messages/en.json
{
  "cart": {
    "items": "{count, plural, =0 {No items} one {# item} other {# items}}",
    "total": "Total: {price}"
  }
}

// messages/zh.json
{
  "cart": {
    "items": "{count} sản phẩm",
    "total": "Tổng: {price}"
  }
}
```

```tsx
function CartSummary({ count, total }: { count: number; total: string }) {
  const t = useTranslations('cart')

  return (
    <div>
      <p>{t('items', { count })}</p>
      <p>{t('total', { price: total })}</p>
    </div>
  )
}
// count=0: "No items" / "0 sản phẩm"
// count=1: "1 item" / "1 sản phẩm"
// count=5: "5 items" / "5 sản phẩm"
```

### Date và number formatting

```tsx
import { useFormatter } from 'next-intl'

function FormattedContent() {
  const format = useFormatter()

  const date = new Date()
  const price = 1234.56

  return (
    <div>
      {/* Date formatting */}
      <p>{format.dateTime(date, { dateStyle: 'long' })}</p>
      {/* en: "January 15, 2024" / vi: "15 tháng 1, 2024" */}

      {/* Relative time */}
      <p>{format.relativeTime(date)}</p>
      {/* en: "2 hours ago" / vi: "2 giờ trước" */}

      {/* Number formatting */}
      <p>{format.number(price, { style: 'currency', currency: 'VND' })}</p>
      {/* en: "₫1,234.56" / vi: "1.234,56 ₫" */}
    </div>
  )
}
```

### Language switcher

```tsx
'use client'

import { useLocale } from 'next-intl'
import { useRouter, usePathname } from 'next/navigation'

function LanguageSwitcher() {
  const locale = useLocale()
  const router = useRouter()
  const pathname = usePathname()

  const switchLocale = (newLocale: string) => {
    const newPath = pathname.replace(`/${locale}`, `/${newLocale}`)
    router.push(newPath)
  }

  return (
    <select
      value={locale}
      onChange={(e) => switchLocale(e.target.value)}
      aria-label="Chọn ngôn ngữ"
    >
      <option value="vi">Tiếng Việt</option>
      <option value="en">English</option>
    </select>
  )
}
```

### Sử dụng trong Server Component

```tsx
// app/[locale]/page.tsx
import { getTranslations } from 'next-intl/server'

export async function generateMetadata({ params: { locale } }) {
  const t = await getTranslations({ locale, namespace: 'home' })

  return {
    title: t('title'),
    description: t('description'),
  }
}

export default async function Page() {
  const t = await getTranslations('home')

  return <h1>{t('title')}</h1>
}
```

### Translation key management

```tsx
// Dùng TypeScript type safety
// Tạo type definition

// types/i18n.d.ts
import en from '../messages/en.json'

type Messages = typeof en

declare global {
  interface IntlMessages extends Messages {}
}
```

### Hướng dẫn cộng tác AI

**Ý định cốt lõi**: Để AI giúp implement kiến trúc internationalization.

**Công thức định nghĩa yêu cầu**:
- Chọn framework: Next.js + [next-intl/react-i18next]
- Ngôn ngữ hỗ trợ: [Tiếng Việt/English/Japanese...]
- Yêu cầu chức năng: [date formatting/plural/gender]

**Ví dụ Prompt**:

```
Vui lòng giúp setup Next.js + next-intl internationalization:
1. Hỗ trợ Tiếng Việt và English
2. Chuyển ngôn ngữ dựa trên URL path (/vi, /en)
3. Bao gồm language switcher component
4. Config date và currency formatting
5. Cung cấp folder structure và config files đầy đủ
```

### Checklist nghiệm thu

- [ ] Text tách ra khỏi code
- [ ] Hỗ trợ language switching
- [ ] Date/number format đúng
- [ ] SEO metadata hỗ trợ đa ngôn ngữ
- [ ] Translation keys có TypeScript type
